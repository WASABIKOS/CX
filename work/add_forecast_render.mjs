import fs from 'node:fs';

const forecastCode = `var targetBySegment={'rNPS / Relación':0,'pNPS Mobile - Prepago':4.9,'pNPS Mobile - Contrato':-1.1,'pNPS Internet':-7.7};var target=targetBySegment[activeSegment]??0;var forecastMonths=Array.from({length:12},function(_,i){return '2026-'+String(i+1).padStart(2,'0')});var actualSeries=forecastMonths.map(function(m){var rows=RESPONSES.filter(function(x){return x.month===m&&(activeSegment==='Total'||x.segment===activeSegment)});return rows.length?calcNps(rows):null});var observed=actualSeries.filter(function(v){return v!==null});var lastObserved=observed.length?observed[observed.length-1]:0;var lastIndex=actualSeries.reduce(function(a,v,i){return v!==null?i:a},-1);var requiredSeries=forecastMonths.map(function(_,i){return lastIndex<0?null:(i<=lastIndex?null:lastObserved+(target-lastObserved)*(i-lastIndex)/(11-lastIndex||1))});var chartW=960,chartH=245,padL=42,padR=18,padT=18,padB=30,plotW=chartW-padL-padR,plotH=chartH-padT-padB,scaleY=function(v){return padT+(100-v)/200*plotH},scaleX=function(i){return padL+i/(forecastMonths.length-1)*plotW},linePath=function(series){return series.map(function(v,i){return v===null?'':(i===0||series[i-1]===null?'M':'L')+scaleX(i).toFixed(1)+' '+scaleY(v).toFixed(1)}).join(' ')};var grid=[100,50,0,-50,-100].map(function(v){return '<line x1="'+padL+'" x2="'+(chartW-padR)+'" y1="'+scaleY(v)+'" y2="'+scaleY(v)+'" stroke="'+(v===0?'#aaa29c':'#403b37')+'" stroke-width="'+(v===0?1.5:1)+'"/><text x="4" y="'+(scaleY(v)+4)+'" fill="#aaa29c" font-size="10">'+v+'</text>'}).join('');var xLabels=forecastMonths.map(function(m,i){return '<text x="'+scaleX(i)+'" y="'+(chartH-7)+'" text-anchor="middle" fill="#aaa29c" font-size="9">'+MONTHS[i]+'</text>'}).join('');var actualPoints=actualSeries.map(function(v,i){return v===null?'':'<circle cx="'+scaleX(i)+'" cy="'+scaleY(v)+'" r="3.5" fill="#ff6b00"><title>'+MONTHS[i]+' 2026: '+v.toFixed(1)+'</title></circle>'}).join('');var requiredPoints=requiredSeries.map(function(v,i){return v===null?'':'<circle cx="'+scaleX(i)+'" cy="'+scaleY(v)+'" r="3" fill="#8db600"><title>'+MONTHS[i]+' 2026 requerido: '+v.toFixed(1)+'</title></circle>'}).join('');$('forecastChart').innerHTML='<svg viewBox="0 0 '+chartW+' '+chartH+'" role="img" aria-label="Forecast NPS 2026">'+grid+xLabels+'<path d="'+linePath(actualSeries)+'" fill="none" stroke="#ff6b00" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/><path d="'+linePath(requiredSeries)+'" fill="none" stroke="#8db600" stroke-width="2.5" stroke-dasharray="7 5" stroke-linecap="round"/><line x1="'+padL+'" x2="'+(chartW-padR)+'" y1="'+scaleY(target)+'" y2="'+scaleY(target)+'" stroke="#fff" stroke-width="1" stroke-dasharray="3 4" opacity=".7"/>'+actualPoints+requiredPoints+'</svg>';$('forecastLegend').innerHTML='<span><i class="actual"></i>Actual NPS</span><span><i class="required"></i>Ritmo requerido</span><span><i class="target"></i>Meta '+target.toFixed(1)+'</span>';$('targetCards').innerHTML='<article class="target-card"><span class="klabel">NPS actual 2026</span><div class="target-value current">'+(observed.length?lastObserved.toFixed(1):'—')+'</div><div class="ksub">'+(latestMonth?monthLabel(latestMonth):'Sin datos')+'</div></article><article class="target-card"><span class="klabel">Meta 2026</span><div class="target-value goal">'+target.toFixed(1)+'</div><div class="ksub">'+activeSegment+'</div></article><article class="target-card"><span class="klabel">Brecha vs meta</span><div class="target-value '+(target-lastObserved>=0?'positive':'negative')+'">'+(target-lastObserved>=0?'+':'')+(target-lastObserved).toFixed(1)+'</div><div class="ksub">Puntos por alcanzar</div></article>';`;

for (const file of ['work/build_dashboard_clean.mjs','outputs/medallia_cx_nps_2026-08-14/medallia_cx_nps_dashboard.html']) {
  let h=fs.readFileSync(file,'utf8');
  const existing=h.indexOf('var targetBySegment=');
  const scriptEnd=h.indexOf('</script></body></html>', existing>=0?existing:0);
  if(scriptEnd<0) throw new Error('script end not found in '+file);
  if(existing>=0) h=h.slice(0,existing)+forecastCode+'}\nrender();\n'+h.slice(scriptEnd);
  else {
    const renderPos=h.indexOf('\nrender();');
    if(renderPos<0) throw new Error('render end not found in '+file);
    h=h.slice(0,renderPos-1)+'\n'+forecastCode+'}\nrender();\n'+h.slice(scriptEnd);
  }
  fs.writeFileSync(file,h);
}
console.log('forecast render added');
